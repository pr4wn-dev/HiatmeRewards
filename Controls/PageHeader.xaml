<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HiatMeApp.Controls.PageHeader"
             x:Name="ThisPageHeader">
    <Border Stroke="Transparent"
            StrokeThickness="0"
            BackgroundColor="#2a2a2a"
            Padding="20,16">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="0,0,16,16" />
        </Border.StrokeShape>
        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16">
            <!-- Icon Container -->
            <Border Grid.Column="0"
                    WidthRequest="56"
                    HeightRequest="56"
                    StrokeThickness="0"
                    BackgroundColor="{Binding AccentColor, Source={x:Reference ThisPageHeader}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="14" />
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{Binding AccentColor, Source={x:Reference ThisPageHeader}}"
                            Offset="0,4"
                            Radius="12"
                            Opacity="0.4" />
                </Border.Shadow>
                <Label Text="{Binding Icon, Source={x:Reference ThisPageHeader}}"
                       FontSize="28"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>
            
            <!-- Text Container -->
            <VerticalStackLayout Grid.Column="1" 
                                 VerticalOptions="Center"
                                 Spacing="2">
                <Label Text="{Binding Title, Source={x:Reference ThisPageHeader}}"
                       FontSize="22"
                       FontAttributes="Bold"
                       TextColor="White"
                       LineBreakMode="TailTruncation" />
                <Label Text="{Binding Subtitle, Source={x:Reference ThisPageHeader}}"
                       FontSize="13"
                       TextColor="#9ca3af"
                       IsVisible="{Binding HasSubtitle, Source={x:Reference ThisPageHeader}}"
                       LineBreakMode="TailTruncation" />
            </VerticalStackLayout>
        </Grid>
    </Border>
</ContentView>






